const ConfigManager = () => {
  return (
    <div>
      <h3 className="text-lg font-semibold">Gestion de la configuration</h3>
      <p>Le contenu pour la gestion de la configuration apparaîtra ici.</p>
      {/* Vous pouvez ajouter ici les éléments de l'interface utilisateur pour gérer la configuration */}
    </div>
  );
};

export default ConfigManager;
